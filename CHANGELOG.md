#### No versioning yet, will add that soon.

- **BIG UPDATE**:
    - openai-cli dependency stopped working, so removed it's requirement.
    - Now calling openai api with a custom bash script instead.
